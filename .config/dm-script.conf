#!/bin/bash
##____  _                      _      
#|  _ \| |__   ___   ___ _ __ (_)_  __
#| |_) | '_ \ / _ \ / _ \ '_ \| \ \/ /
#|  __/| | | | (_) |  __/ | | | |>  < 
#|_|   |_| |_|\___/ \___|_| |_|_/_/\_\
#                                     
# -*- coding: utf-8 -*-
#
# Configuration file for my dm script
# Here all my configuration for my run menu script will be stored
# So I can just import all settings and use it accross all my various
# Dmenu scripts

function error () {

clear; printf "ERROR:\\n%s\\n" "$1" >&2; exit 1;

}


# check if you use wayland or xorg
if [ $XDG_SESSION_TYPE = "wayland" ] ; then

function run(){

    wofi --show dmenu

}

else

function run(){

    rofi -dmenu
}

fi

# Choose your preferred browser
BROWSER=librewolf
FULLSCREEN_BROWSER=brave-browser
URL="$HOME/.config/qutebrowser/quickmarks"

# PDF viewer of choice
PDF_VIEWER=zathura

# Text editors of choice
GUI_EDITOR="emacsclient -c -a emacs"
TUI_EDITOR=nvim

# Choose applications you wanna be able to run with dm-app
applications=$(echo "qutebrowser" "$applications" )
applications=$(echo "timeshift-launcher" "$applications")
applications=$(echo "gparted" "$applications")
applications=$(echo "spotify" "$applications")     
applications=$(echo "gimp" "$applications")    
applications=$(echo "steam" "$applications")    
applications=$(echo "kdenlive" "$applications")    
applications=$(echo "celluloid" "$applications")    
applications=$(echo "neovide" "$applications")
applications=$(echo "emacs" "$applications")


# SSH User
USER_SSH=karl

# Terminal of choice
TERM=kitty
